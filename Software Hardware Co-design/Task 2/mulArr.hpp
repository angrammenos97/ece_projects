#pragma once

#define ln 4
#define lm 4
#define lp 4

#define n (1 << ln)
#define m (1 << lm)
#define p (1 << lp)

const unsigned int n_len = n;
const unsigned int m_len = m;
const unsigned int p_len = p;
